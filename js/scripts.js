
function validate() {
 var a= document.getElementByName("choice");
 for (var a[i] = creating set of instructions) {
   if (a[i].checked) {
     if( a[i].value == "creating set of instruction" )
        alert("your answer is correct");
     else
        alert("your answer is not correct");
   }
  }
 }
